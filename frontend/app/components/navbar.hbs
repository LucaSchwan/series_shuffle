<nav class="flex w-screen bg-gray-600">
    <LinkTo class="flow-left" @route="index">
        <h1 class="m-4 font-bold text-3xl text-gray-200">Series Shuffle</h1>
    </LinkTo>
    {{#if this.session.isAuthenticated}}
        <button class="bg-red-500 hover:bg-red-600 m-4 p-2 rounded-md shadow-xl right-0 absolute" type="button" onClick={{this.logout}}>Logout</button>
    {{/if}}
</nav>
